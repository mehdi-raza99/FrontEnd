// tailwind.config.js
module.exports = {
    content: [
      "./**/*.html", // Add paths to all HTML files or other file types where Tailwind classes are used
      // If you're using JSX, you might also include:
      // "./**/*.jsx", 
      // "./**/*.js", 
      // etc.
    ],
    theme: {
      extend: {},
    },
    plugins: [],
  }
  